# VPS Connection Configuration
# Shared by audit.sh, compose.sh, and provision.sh

VPS_HOST=<SSH config alias of VPS on local machine>
VPS_USER=<user on remote machine>
VPS_HOSTNAME=<hostname on remote machine>
VPS_TIMEZONE=<timezone on remote machine>
